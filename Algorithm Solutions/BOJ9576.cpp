#include <iostream>

using namespace std;

int main(void) {
    int TC;
    cin >> TC;

    for (int tcCnt = 0; tcCnt < TC; ++tcCnt) {
        int N, M;
        cin >> N >> M;

        for (int i = 0; i < M; ++i) {
            int a, b;
            cin >> a >> b;
            
            
        }
    }

    return 0;
}

