#define WIDTH (8)
#define HEIGHT (8)

int (*tiles)[WIDTH];

void init(int N, int mTiles[][WIDTH])
{
	tiles = mTiles;
	while (canScore(mTiles)) {
		
	}
}

int mRet[5];
int* takeTurn()
{
	return mRet;
}


bool canScore(int mTiles[][WIDTH]) {
	
}