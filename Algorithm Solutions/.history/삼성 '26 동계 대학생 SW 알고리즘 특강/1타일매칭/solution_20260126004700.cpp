#define WIDTH (8)
#define HEIGHT (8)

int (*tiles)[WIDTH];

void init(int N, int mTiles[][WIDTH])
{
	tiles = mTiles;
	for (int col=0; col<WIDTH; ++col) {
		// 각 열 상하 연속 지점 찾기
	}

	for (int )
}

int mRet[5];
int* takeTurn()
{
	return mRet;
}