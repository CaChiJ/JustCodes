#define WIDTH (8)
#define HEIGHT (8)

int (*tiles)[WIDTH];

void init(int N, int mTiles[][WIDTH])
{
	tiles = mTiles;
	while (true) {
		if (canScore(tiles)) {
			
		}
	}
}

int mRet[5];
int* takeTurn()
{
	return mRet;
}


bool canScore(int mTiles[][WIDTH]) {
	
}